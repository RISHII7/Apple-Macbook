import { navLinks } from './nav-links';
import { noChangeParts } from './no-change-parts';
import { performanceImages } from './performance-images';
import { performanceImgPositions } from './performance-image-positions';
import { features } from './features';
import { featureSequence } from './feature-sequence';
import { footerLinks } from './footer-links';

export {
  navLinks,
  noChangeParts,
  performanceImages,
  performanceImgPositions,
  features,
  featureSequence,
  footerLinks,
};
